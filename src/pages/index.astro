---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('posts')).sort((a, b) =>
  b.data.pubDate.getTime() - a.data.pubDate.getTime()
);

const totalPosts = posts.length;

const homePosts = posts.map((post) => ({
  ...post,
  previewTags: post.data.tags.slice(0, 3),
  summaryPreview:
    post.data.summary.length > 110 ? `${post.data.summary.slice(0, 110).trim()}…` : post.data.summary
}));
---

<BaseLayout title="swrited | Blog" description="swrited 的个人博客，记录 AI、工程、部署与产品实践。">
  <section class="hero hero-grid">
    <div class="hero-copy-block">
      <div class="eyebrow">PERSONAL LOG · BUILD / BREAK / RECOVER</div>
      <h1>把折腾、踩坑和复盘，写成以后还能复用的东西。</h1>
      <p class="hero-copy">
        这里记录我做过的 agent workflow、模型接口、部署链路和故障复盘。不是只展示结果，
        也会把过程、失误和最后留下来的方法一起写下来。
      </p>
      <div class="hero-actions">
        <a class="button button-dark" href="#all-posts">直接看文章</a>
        <a class="button" href="/about">认识我</a>
      </div>
    </div>

    <aside class="hero-panel card">
      <div class="hero-panel-top">
        <span class="eyebrow">NOW / FOCUS</span>
        <span class="hero-panel-status">online</span>
      </div>
      <h2>最近在把能跑通的链路，变成能复盘的经验。</h2>
      <p>
        关注的方向主要还是 NewAPI、AI Infra、安全事件复盘，以及那些真正会在生产环境里出问题的细节。
      </p>
      <div class="hero-stack">
        <div>
          <span class="hero-label">CURRENTLY BUILDING</span>
          <p>多 agent 协作、博客迭代、部署与运维记录。</p>
        </div>
        <div>
          <span class="hero-label">WRITING STYLE</span>
          <p>尽量真实、可复盘、可复用，少一点空话。</p>
        </div>
      </div>
    </aside>
  </section>

  <section class="home-strip card">
    <div class="home-strip-item">
      <span class="hero-label">SITE SIGNAL</span>
      <p>这不是模板展示页，而是持续更新的工作台与留档簿。</p>
    </div>
    <div class="home-strip-item">
      <span class="hero-label">RECENT THREADS</span>
      <p>AI infra、agent orchestration、部署链路、事故排查。</p>
    </div>
    <div class="home-strip-item">
      <span class="hero-label">DEFAULT MODE</span>
      <p>先跑通，再复盘，再把过程写清楚。</p>
    </div>
  </section>

  <section class="section-head" id="all-posts">
    <div>
      <span class="eyebrow">ALL POSTS</span>
      <h2>全部文章</h2>
      <p class="section-copy">从真实项目、部署链路到故障复盘，按时间顺序直接展开。</p>
    </div>
    <div class="section-summary card section-summary-card">
      <span class="section-summary-number">{String(totalPosts).padStart(2, '0')}</span>
      <span class="section-summary-label">篇已发布文章</span>
    </div>
  </section>

  <section class="post-list">
    {homePosts.map((post) => (
      <a class="card post-card" href={`/blog/${post.slug}`}>
        {post.data.cover && (
          <img
            class="post-cover"
            src={post.data.cover}
            alt={post.data.coverAlt ?? post.data.title}
            loading="lazy"
          />
        )}
        <div class="post-card-body">
          <div class="post-meta-row">
            <div class="post-meta">
              <span>{post.data.pubDate.toLocaleDateString('zh-CN')}</span>
            </div>
            {post.previewTags.length > 0 && (
              <div class="tag-row post-tags">
                {post.previewTags.map((tag) => (
                  <span class="tag">{tag}</span>
                ))}
              </div>
            )}
          </div>
          <h3>{post.data.title}</h3>
          <p>{post.summaryPreview}</p>
          <div class="post-card-footer">
            <span class="text-link">继续阅读 →</span>
          </div>
        </div>
      </a>
    ))}
  </section>
</BaseLayout>
